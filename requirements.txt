# Core
httpx>=0.27

# Tools
ddgs>=1.0          # web search backend (formerly `duckduckgo_search`)
duckduckgo_search>=6.0  # fallback for older pins; the tool tries `ddgs` first

# Web UI (`python -m jarvis.web`) -- voice runs in the browser, no extra audio deps needed here
fastapi>=0.110
uvicorn[standard]>=0.29

# Voice (optional but needed for `python -m jarvis.main`)
faster-whisper>=1.0
pyttsx3>=2.90
sounddevice>=0.4
numpy>=1.26
openwakeword>=0.6  # only needed for --wake-word mode

# Dev/test
pytest>=8.0
